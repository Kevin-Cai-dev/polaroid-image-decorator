export const Disclaimer = () => {
    return (
        <p className="border-l-4 border-base-300 pl-4 italic">
            {
                "✨ If you run into a fetch error, most likely the backend isn't running ¯\\_(ツ)_/¯"
            }
        </p>
    );
};
